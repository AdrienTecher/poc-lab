// Replace this with the React component downloaded from claude.ai.
// It must `export default` a component; add its dependencies (e.g. recharts)
// to this app's package.json.
export default function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>New app</h1>
      <p>Replace src/App.jsx with the downloaded artifact component.</p>
    </main>
  );
}
